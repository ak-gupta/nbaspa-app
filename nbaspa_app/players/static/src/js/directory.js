/**
 * @module directory Code for generating the player directory and search
 */

class PlayerDirectory extends PlayerSearch {
    constructor(inputTag, divTag) {
        super(inputTag, divTag, "100px")
    }
}
